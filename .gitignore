node_modules/
dist/
dist-ssr/
*.local

# Build / tool caches
.vite/
coverage/
*.tsbuildinfo

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editors and OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Local env overrides only. The committed .env / .env.pages / .env.domain files hold
# build configuration (VITE_BASE), not secrets, and are deliberately tracked.
.env.local
.env.*.local

# Synced from node_modules by scripts/sync-maplibre-worker.mjs
public/maplibre/
